<UserControl x:Class="QRTestWpf.Views.ReturnScanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="450">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="12*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="18*"/>
        </Grid.RowDefinitions>

        <!-- 물결 모양 Path -->
        <Grid Grid.Row="0">
            <Path Fill="#FFA500" Stretch="Fill" Height="223" VerticalAlignment="Top">
                <Path.Data>
                    <PathGeometry>
                        <PathFigure StartPoint="0,100">
                            <BezierSegment Point1="150,0" Point2="300,200" Point3="450,100" />
                            <LineSegment Point="450,0" />
                            <LineSegment Point="0,0" />
                            <LineSegment Point="0,100" />
                        </PathFigure>
                    </PathGeometry>
                </Path.Data>
            </Path>
        </Grid>

        <!-- 이름 -->
        <Grid Grid.Row="1" VerticalAlignment="Center" Height="64">
            <StackPanel>
                <TextBlock Text="반납할 책을"
                       FontSize="25"  FontWeight="Bold"
                       Foreground="#FFA500" 
                       HorizontalAlignment="Center"/>

                <TextBlock Text="밑에 리더기에 넣어주세요"
                           FontSize="25"  FontWeight="Bold"
                           Foreground="#FFA500"
                           HorizontalAlignment="Center"/>
            </StackPanel>

        </Grid>

        <!-- 상태 메시지 -->
        <Grid Grid.Row="2">
            <Image Source="/Resources/librarian.png" Width="250" Height="250" />
        </Grid>

        <Button Content="화면 전환 확인용" Margin="304,88,10,32"
                Command="{Binding BoxCommand}"></Button>
    </Grid>
</UserControl>
