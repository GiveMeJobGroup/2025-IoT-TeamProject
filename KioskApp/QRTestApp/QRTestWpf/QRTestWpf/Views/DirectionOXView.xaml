<UserControl x:Class="QRTestWpf.Views.DirectionOXView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
             xmlns:local="clr-namespace:QRTestWpf.Views"
             Dialog:DialogParticipation.Register="{Binding}"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="450">
    <!--둥근 버튼-->
    <UserControl.Resources>
        <Style TargetType="Button">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="15"/>
                </Style>
            </Style.Resources>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="200*"/>
            <RowDefinition Height="45*"/>
            <RowDefinition Height="90*"/>
        </Grid.RowDefinitions>

        <!-- 책 정보 영역 -->
        <Border Grid.Row="0" BorderBrush="#FFA500" BorderThickness="2" Padding="15" CornerRadius="4" Margin="40">
            <StackPanel>
                <TextBlock Text="제목 : 어린왕자" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                <TextBlock Text="내용:" FontWeight="Bold"/>
                <TextBlock Text="어린왕자~" Margin="10,0,0,0"/>
                <TextBlock Text="장미~" Margin="10,0,0,0"/>
                <TextBlock Text="코끼리먹은 보아뱀~" Margin="10,0,0,10"/>
                <TextBlock>
                    <Run Text="위치 : " FontWeight="Bold"/>
                    <Run Text="8AA3DE-1" FontWeight="Bold"/>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- 안내 질문 -->
        <TextBlock Grid.Row="1"
                   Text="도서 위치 안내를 받으시겠습니까?"
                   Foreground="#FFA500"
                   FontWeight="Bold"
                   FontSize="22"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"/>

        <!-- 버튼 영역 -->
        <StackPanel Grid.Row="2" Orientation="Vertical" VerticalAlignment="Center">
            <Button Content="예" 
                    BorderBrush="#FFA500" Foreground="#FFA500" Background="Transparent"
                    FontWeight="Bold"  Height="50"  FontSize="20" BorderThickness="2"
                    Margin="10"
                    Command="{Binding DirectionOCommand}"/>

            <Button Content="아니오" 
                    Background="#FFA500" Foreground="White" FontWeight="Bold"
                    Height="50" FontSize="20" BorderThickness="0"
                    Margin="10"
                    Command="{Binding DirectionXCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>