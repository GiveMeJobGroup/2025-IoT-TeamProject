<UserControl x:Class="QRTestWpf.Views.ReturnBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="450">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60*"/>
            <RowDefinition Height="68*"/>
            <RowDefinition Height="47*"/>
        </Grid.RowDefinitions>

        <!-- 물결 모양 Path -->
        <Grid Grid.Row="0">
            <Path Fill="#FFA500" Stretch="Fill" Height="223" VerticalAlignment="Top">
                <Path.Data>
                    <PathGeometry>
                        <PathFigure StartPoint="0,100">
                            <BezierSegment Point1="150,0" Point2="300,200" Point3="450,100" />
                            <LineSegment Point="450,0" />
                            <LineSegment Point="0,0" />
                            <LineSegment Point="0,100" />
                        </PathFigure>
                    </PathGeometry>
                </Path.Data>
            </Path>
        </Grid>

        <Grid Grid.Row="1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="49*"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Image Source="/Resources/Box.png" Grid.RowSpan="2" />

        </Grid>

        <!-- 상태 메시지 -->
        <Grid Grid.Row="2">
            <TextBlock Text="책을 반납함에 넣어주세요"
                       FontSize="25"  FontWeight="Bold"
                       Foreground="#FFA500" 
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Grid>

        <Button Content="화면 전환 확인용" Margin="304,88,10,32"
                Command="{Binding WaitCommand}"></Button>
    </Grid>
</UserControl>
